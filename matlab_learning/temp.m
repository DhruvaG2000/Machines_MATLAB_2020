function output = temp(d,t)
%
% description.
%
% @since 1.0.0
% @param {type} [name] description.
% @return {type} [name] description.
% @see dependencies
%
    output = 5 + (2*(ceil(d)-1)) + (0.25*ceil(t));

end
